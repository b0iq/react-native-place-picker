<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme: Dark mode -->
    <style name="Theme.PlacePickerTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">#222</item> <!-- Toolbar background: dark (Dark mode) -->
        <item name="colorOnPrimary">@color/colorOnPrimary</item> <!-- Icon/text color: white (Dark mode) -->
        <!-- ActionBar/Toolbar style overrides -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:toolbarStyle">@style/MyToolbar</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Custom ActionBar Style -->
    <style name="MyActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>
    <style name="MyActionBarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/colorOnPrimary</item>
    </style>
    <style name="MyToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextColor">@color/colorOnPrimary</item>
        <item name="subtitleTextColor">@color/colorOnPrimary</item>
    </style>
</resources>
